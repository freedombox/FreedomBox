# Tahoe-LAFS Storage Node web interface

# See if Tahoe-LAFS has basic auth
# Enable Tahoe only from localhost
# Do basic auth and LDAP from apache reverse proxy

Listen 5678

<VirtualHost *:5678>
      ProxyPass / http://localhost:1234/
</VirtualHost>